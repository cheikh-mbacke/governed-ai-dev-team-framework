{
  "terminalAllowlist": [
    "git status",
    "git diff",
    "git log",
    "git show",
    "git branch",
    "git add",
    "git commit",
    "git rev-parse",
    "git merge-base",
    "Get-ChildItem",
    "Select-String",
    "Test-Path",
    "Get-Content",
    "Write-Output",
    "Get-Location",
    "Get-Date",
    "pwd",
    "ls",
    "python -m pytest",
    "python -m ruff",
    "python scripts/ai-team/validate.py",
    "python scripts/ai-team/sync_source_manifest.py",
    "python scripts/ai-team/check_git_policy.py",
    "python scripts/ai-team/diagnose.py",
    "py -3 -m pytest",
    "py -3 -m ruff",
    "py -3 scripts/ai-team/validate.py",
    "py -3 scripts/ai-team/sync_source_manifest.py",
    "py -3 scripts/ai-team/check_git_policy.py",
    "py -3 scripts/ai-team/diagnose.py"
  ],
  "autoRun": {
    "allow_instructions": [
      "Allow read-only repository inspection commands.",
      "Allow project-local unit, integration, lint, typecheck, formatting, and build commands when they do not target production or mutate protected infrastructure.",
      "Allow framework fabrication scripts: validate.py, sync_source_manifest.py, check_git_policy.py, diagnose.py.",
      "Allow starting, stopping, or restarting a local development or test server bound to localhost or a project-configured local port when it does not target production or a publicly reachable environment.",
      "Allow local browser automation for UI testing via the webapp-testing skill (Playwright scripts, headless chromium) against a local server only.",
      "Allow installing dependencies already declared in the project's own manifest via the project's setup command from project-profile.yaml."
    ],
    "block_instructions": [
      "Require explicit approval for any command that pushes directly to main, master, trunk, or another protected branch.",
      "Require explicit approval for destructive Git history operations such as force push, reset --hard, force-deleting a branch, or deleting protected branches.",
      "Require explicit approval and recorded evidence impact before git commit --amend, git rebase, or another history rewrite.",
      "Never create a commit or stage changes on main, master, trunk, or the project-configured protected branch; use a short-lived feature branch.",
      "Require explicit approval for production deployments or commands targeting a production environment.",
      "Require explicit approval for production database writes, destructive migrations, or data deletion.",
      "Require explicit approval for Kubernetes, Terraform, cloud, IAM, permission, secret, or credential mutations.",
      "Require explicit approval for destructive filesystem commands such as recursive deletion outside build/temp directories.",
      "Require explicit approval for adding a new dependency that is not already declared in the project's manifest.",
      "Require explicit approval for starting any server or process bound to a non-local interface, a non-project-configured port, or reachable outside the local machine."
    ]
  }
}
