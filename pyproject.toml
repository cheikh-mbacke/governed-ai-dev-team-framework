[project]
name = "governed-ai-dev-team-framework"
version = "0.1.0"
description = "Human-governed multi-agent software development framework for Cursor"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
  "jsonschema>=4.20"
]

[tool.ruff]
line-length = 100
