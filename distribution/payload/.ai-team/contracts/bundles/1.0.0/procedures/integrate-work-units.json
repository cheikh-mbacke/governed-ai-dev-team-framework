{
  "procedure_id": "integrate-work-units", "revision": "1.0.0", "intent": "Assess cross-Work-Unit coherence before a bounded merge into the Run integration branch.", "invocation_mode": "explicit_only",
  "required_inputs": ["candidate_sha", "integration_sha", "shared_contracts", "verification_evidence"],
  "steps": ["Review shared contracts and dependency ordering", "Approve or block the proposed integration merge", "Require complete revalidation evidence after any accepted merge"],
  "required_outputs": ["integration_review", "merge_disposition", "revalidation_requirements"],
  "invariants": ["No protected branch merge", "No invented shared contract", "Conflicts have bounded retries"]
}
