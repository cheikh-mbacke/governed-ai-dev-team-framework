# Requirements

- Functional and non-functional requirements — what behavior is expected?
- Functional and technical specifications — what detail is needed to avoid an arbitrary interpretation?

Document **27**: area filter (`allowed_areas`) on `RunAuthorizationGrant` for planned / unattended runs.
