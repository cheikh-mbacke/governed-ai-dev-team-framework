# Example: Project A

This example shows the **shape** of an initialized project after the human material has been registered and the Project Compiler has produced an execution model.

It is not copied by the installer.
